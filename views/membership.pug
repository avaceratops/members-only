extends layout

include mixins/formField
include mixins/submitButton

block content
  section.mx-auto.flex.max-w-md.rounded-lg.bg-white.p-4
    form.flex.grow.flex-col.gap-8(method='POST')
      +formField('What is the capital of France?', 'text', 'membershipPassword', null, errors)(
        required
      )

      +submitButton('Submit', 'submit', 'blue').col-span-2
